<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       x:Class="SongManager.Desktop.Pages.Users.UserPage"
                       xmlns:pages="clr-namespace:SongManager.Desktop.Pages"
                       xmlns:pageModel="clr-namespace:SongManager.Desktop.PageModels"
                       x:TypeArguments="pageModel:UserPageModel"
                       Shell.TabBarIsVisible="False"
                       Background="{StaticResource PageBackgroundGradient}">

    <Grid Padding="24,18,24,0" RowDefinitions="Auto,*,Auto" RowSpacing="14" x:Name="PageRoot">
        <Label Text="♬" Style="{StaticResource WatermarkLabel}" HorizontalOptions="End" VerticalOptions="Start" Margin="0,-44,-24,0" />

        <Border x:Name="HeroCard" Style="{StaticResource GlassCard}">
            <VerticalStackLayout Spacing="6">
                <Label Text="Administración de Usuarios" Style="{StaticResource HeroTitle}" />
                <Label Text="Crear y editar usuarios. Solo administradores pueden realizar mantenimiento." Style="{StaticResource HeroSubtitle}" />
            </VerticalStackLayout>
        </Border>

        <Grid Grid.Row="1" RowDefinitions="Auto,*" RowSpacing="12" x:Name="UsersInfoGrid">
            <Border Style="{StaticResource GlassCard}" IsVisible="{Binding CanManageUsers}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{Binding EditorTitle}" FontAttributes="Bold" FontSize="20" />

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                        <Border Style="{StaticResource FieldShell}"><Entry Placeholder="Usuario" Text="{Binding CurrentUser.Username}" /></Border>
                        <Border Grid.Column="1" Style="{StaticResource FieldShell}"><Entry Placeholder="Correo" Keyboard="Email" Text="{Binding CurrentUser.Email}" /></Border>
                        <Border Grid.Row="1" Style="{StaticResource FieldShell}"><Entry Placeholder="Primer nombre" Text="{Binding CurrentUser.FirstName}" /></Border>
                        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldShell}"><Entry Placeholder="Segundo nombre" Text="{Binding CurrentUser.SecondName}" /></Border>
                        <Border Grid.Row="2" Style="{StaticResource FieldShell}"><Entry Placeholder="Primer apellido" Text="{Binding CurrentUser.FirstSurname}" /></Border>
                        <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource FieldShell}"><Entry Placeholder="Segundo apellido" Text="{Binding CurrentUser.SecondSurname}" /></Border>
                        <Border Grid.Row="3" Style="{StaticResource FieldShell}"><Entry Placeholder="Contraseña" IsPassword="True" Text="{Binding CurrentUser.Password}" /></Border>
                        <HorizontalStackLayout Grid.Row="3" Grid.Column="1" Spacing="8" VerticalOptions="Center">
                            <Label Text="Administrador" VerticalOptions="Center" />
                            <Switch IsToggled="{Binding CurrentUser.IsAdmin}" />
                        </HorizontalStackLayout>
                    </Grid>

                    <HorizontalStackLayout Spacing="8">
                        <Button Text="Guardar" Style="{StaticResource PrimaryActionButton}" Command="{Binding SaveUserCommand}" />
                        <Button Text="Nuevo" Style="{StaticResource SecondaryActionButton}" Command="{Binding PrepareNewUserCommand}" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource GlassCard}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Usuarios registrados" FontAttributes="Bold" FontSize="18" />
                    <CollectionView ItemsSource="{Binding Users}" SelectionMode="None" Margin="0,4,0,0">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource FieldShell}" Margin="0,0,0,12">
                                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="4">
                                        <Label Text="{Binding FullName}" FontAttributes="Bold" />
                                        <Border Grid.Column="1" Style="{StaticResource SoftChip}">
                                            <Label Text="{Binding Username}" FontSize="12" />
                                        </Border>
                                        <Label Grid.Row="1" Text="{Binding Email}" Opacity="0.8" />
                                        <Button Grid.Row="1"
                                                Grid.Column="1"
                                                Text="Editar"
                                                IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CanManageUsers}"
                                                Style="{StaticResource SecondaryActionButton}"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditUserCommand}"
                                                CommandParameter="{Binding .}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <Border Grid.Row="2" x:Name="BottomCard" Style="{StaticResource FloatingBottomBar}">
            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
                <Button Text="Canciones" Style="{StaticResource SecondaryActionButton}" Clicked="GoSongs_Clicked" />
                <Button Grid.Column="1" Text="Cantantes" Style="{StaticResource SecondaryActionButton}" Clicked="GoSingers_Clicked" />
                <Button Grid.Column="2" Text="Usuarios" Style="{StaticResource PrimaryActionButton}" Clicked="GoUsers_Clicked" />
            </Grid>
        </Border>
    </Grid>
</pages:BaseContentPage>
