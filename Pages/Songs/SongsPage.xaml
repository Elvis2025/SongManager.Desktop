<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       x:Class="SongManager.Desktop.Pages.Songs.SongsPage"
                       xmlns:pages="clr-namespace:SongManager.Desktop.Pages"
                       xmlns:pageModel="clr-namespace:SongManager.Desktop.PageModels"
                       xmlns:resources="clr-namespace:SongManager.Desktop.Resx"
                       xmlns:icon="clr-namespace:SongManager.Desktop.UsesCases.IconFont"
                       xmlns:controls="clr-namespace:SongManager.Desktop.Pages"
                       xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                       x:TypeArguments="pageModel:SongsPageModel"
                       Title=""
                       >
    <Grid Padding="10,0,10,10"
          RowDefinitions="Auto,*,Auto"
          
          >
        <Label Text="Listado de canciones"
     FontAttributes="Bold"
     FontSize="36"
     HorizontalOptions="Center"
     VerticalOptions="Center"
     Margin="0,0,0,20"
        Grid.Row="0"
     TextColor="{AppThemeBinding Dark={StaticResource LightBackground},Light={StaticResource Gray600}}"
     />



        <Border Padding="0" Grid.Row="1" Margin="160,0,160,0">
            <Grid RowDefinitions="Auto,*" BackgroundColor="White"
              >

                <Grid ColumnDefinitions="2*,3*,2*,1*,1*"
                      Padding="5"
                      ColumnSpacing="25"
                      Grid.Row="0"
                      BackgroundColor="{AppThemeBinding Dark={StaticResource Gray600}, Light={StaticResource Gray300}}">
                    <Label Text="Cantante" 
                           Grid.Column="0" 
                           Margin="18,0,0,0"
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Dark={StaticResource LightBackground}, Light={StaticResource Gray600}}"
                           />
                    <Label Text="Nombre de la canción" 
                           FontAttributes="Bold" 
                           Grid.Column="1" 
                           TextColor="{AppThemeBinding Dark={StaticResource LightBackground}, Light={StaticResource Gray600}}"/>
                    <Label Text="Autor" 
                           Grid.Column="2" 
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Dark={StaticResource LightBackground}, Light={StaticResource Gray600}}"/>

                    <Label Text="Tono"
                           Grid.Column="3" 
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="{AppThemeBinding Dark={StaticResource LightBackground}, Light={StaticResource Gray600}}"/>
                </Grid>

                <ScrollView Grid.Row="1">
                    <CollectionView ItemsSource="{Binding SongsList}">
                    
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="2*,3*,2*,1.2*,1*" 
                            Padding="5" 
                            BackgroundColor="White"
                          
                            ColumnSpacing="25"
                            >
                                <Label Text="{Binding Singer}" 
                                Grid.Column="0"
                                FontAttributes="Bold"
                               Margin="18,0,0,0"
                                TextColor="{AppThemeBinding Dark={StaticResource Gray400}, Light={StaticResource Gray400}}"
                                />

                                <Label Text="{Binding SongName}" 
                                TextColor="{AppThemeBinding Dark={StaticResource Gray400}, Light={StaticResource Gray400}}"
                                FontAttributes="Bold"
                                Grid.Column="1"
                                />


                                <Label Text="{Binding SongAuthor}" 
                                Grid.Column="2"
                                TextColor="{AppThemeBinding Dark={StaticResource Gray400}, Light={StaticResource Gray400}}"
                                FontAttributes="Bold"
                           
                                />

                                <Label Text="{Binding KeySignatureValue}" 
                                Grid.Column="3"
                                FontAttributes="Bold"
                                TextColor="{AppThemeBinding Dark={StaticResource Gray400}, Light={StaticResource Gray400}}"
                                HorizontalOptions="Center"
                                />
                                    <Label Text="{Static icon:FluentUI.delete_48_regular}"
                                            FontFamily="{Static icon:FluentUI.FontFamily}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Center"
                                            FontAttributes="Bold"
                                           FontSize="22"
                                           Margin="20,0,22,0"
                                           Grid.Column="4"
                                           TextColor="PaleVioletRed"
                                            >
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteSingerCommand}"
                                  CommandParameter="{Binding .}"
                                  />
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Static icon:FluentUI.edit_48_regular}"
                                            FontFamily="{Static icon:FluentUI.FontFamily}"
                                         HorizontalOptions="End"
                                           VerticalOptions="Center"
                                            FontAttributes="Bold"
                                           FontSize="22"
                                           Margin="20,0,62,1"
                                           Grid.Column="4"
                                           TextColor="DarkSlateBlue"
                                            >
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditSingerCommand}"
                                  CommandParameter="{Binding .}"
                                  />
                                        </Label.GestureRecognizers>
                                    </Label>
                                </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                </ScrollView>
            </Grid>
        </Border>

        <Border HorizontalOptions="End"
          VerticalOptions="End"
          StrokeShape="RoundRectangle 100"
          Padding="9.5,9.5,6,-3"
          Margin="0,30,30,30"
          Grid.Row="2"
          >

            <Label Text="{Static icon:FluentUI.add_12_regular}"
             FontFamily="{Static icon:FluentUI.FontFamily}"
             VerticalOptions="Center"
             FontAttributes="Bold"
             FontSize="38"
             HorizontalTextAlignment="Center"
             VerticalTextAlignment="Center"
             Margin="0,0,0,0"
             Padding="0"
             HorizontalOptions="Center"/>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding  CreateSongCommand}"/>
            </Border.GestureRecognizers>
        </Border>
    </Grid>
</pages:BaseContentPage>